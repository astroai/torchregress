"""Microbenchmarks for torchregress losses.

Each module is a standalone script runnable as ``python -m
tools.benchmarks.<name>``.  ``python -m tools.benchmarks`` (the package
``__main__``) runs the full suite.
"""
